{
  "name": "games50-js",
  "version": "1.0.0",
  "author": "yeoji",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "watch": "parcel $GAME/index.html",
    "copy:assets": "mkdir -p dist/assets && cp -r $GAME/assets dist",
    "dev": "yarn copy:assets && yarn watch"
  },
  "dependencies": {
    "phaser": "3.24.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
